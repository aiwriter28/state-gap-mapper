# Copy to .env.local and fill in. Never commit the real .env* file (see .gitignore).

# OpenAI — the app extracts the state machine and ranks gaps via GPT-5.6 structured outputs.
OPENAI_API_KEY=sk-your-key-here
