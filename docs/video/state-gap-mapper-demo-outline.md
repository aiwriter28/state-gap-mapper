# State Gap Mapper canonical demo outline

Final: 2:45, 1920x1080, 30 fps, narrated at natural speed with ElevenLabs voice `u4HtmbcjVZVpiJLQ2GZn`.

| Beat | Visual | Point |
| --- | --- | --- |
| Problem | Blueprint title card, then the fresh production workspace | Happy-path specs leave expensive state questions unanswered. |
| Input | Fresh workspace, then a camera push to the cached-sample controls | Paste prose or use instant offline samples. |
| Map | Wide production checkout machine, then a canvas close-up | GPT-5.6 extracts; deterministic TypeScript detects every structural hole. |
| Evidence | Continuous move from the canvas to `processing x cancel` and its sentence chips | Findings are traceable to the source spec. |
| Decide | Camera push into the real acceptance dialog | The human chooses the intended target. |
| Output | Camera move to the resolved edge and Given-When-Then test stub | A decision becomes reviewable engineering output. |
| Suggest | Amber close-up of the real account-signup suggestion at 94% Confidence | Suggested Events are visually and semantically separate from Structural Gaps. |
| Cascade | Redline close-up of the 8-to-11 cascade after accepting `code_expired` | A creative suggestion feeds deterministic analysis. |
| Edit | Machine-inspector close-up, added-edge close-up, then a move to `GAPS (10)` | Canvas edits validate and recompute immediately with no model call. |
| Architecture | Purpose-built Remotion split diagram | GPT-5.6 supplies judgment; TypeScript protects completeness. |
| Codex | Purpose-built Remotion collaboration card | Codex helped implement, test, harden, deploy, and verify the product. |
| Close | Product name, live URL, and concise value statement | Prose in; missing behavior and testable decisions out. |
